document.addEventListener('DOMContentLoaded', function () {
    ducument.getElementById('form-sorteador').addEventListener('submit', function () {
        let numeroMaximo = ducument.getElementById('numero-maximo');
        numeroMaximo = parseInt(numeroMaximo);
    })
})